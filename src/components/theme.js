const theme = {
  primaryColor: "#1890ff", // Blue (Primary)
  secondaryColor: "#f0f2f5", // Light gray (Background)
  textColor: "#333333", // Dark gray (Text)
  headerColor: "#ffffff", // White (Header)
  footerColor: "#fafafa", // Lightest gray (Footer)
};

export default theme;
